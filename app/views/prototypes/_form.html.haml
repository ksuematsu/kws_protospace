.row.proto-comments
  %h4.col-md-12
    Comments
    %span.num (#{ @comments.length })
    .comments
      = render partial: 'comment', collection: @comments
  .col-md-8
    .form-inline
      = form_for([@prototype, @comment], remote: true) do |f|
        - if signed_in?
          %h4 Write a comment
          .row
            .form-group.col-md-10
              = f.text_area :comment, placeholder: "Your comments", rows: "3", class: "comment_form"
            .form-group.col-md-2
              %button.btn.btn-primary Comment

